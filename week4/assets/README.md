# 指导书资产

`stm32_joint_mapping.png` 来自第四周指导书 1.9 附近的接线示意图。图中给出的接口映射为：

- STM32 接口 3 -> 关节 6
- STM32 接口 4 -> 关节 2
- STM32 接口 5 -> 关节 5
- STM32 接口 6 -> 关节 4
- STM32 接口 7 -> 关节 3
- STM32 接口 8 -> 关节 1
- 接口 1、2 预留

指导书还引用 `animator_3d.py` 和 `airplane_geometry.py`，但当前工作区没有对应源文件；
其接口说明不能替代完整实现。实验 1.8 的动画是选做项，因此不影响必做的矩阵计算。

